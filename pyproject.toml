[project]
name = "lumen-store"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.0.3",
    "Jinja2==3.1.4",
    "Werkzeug==3.0.3",
    "itsdangerous==2.2.0",
]

[tool.uv]
package = false

[tool.vercel]
entrypoint = "run:app"
